from setuptools import setup, find_packages

setup(
    name="compositing_pipeline_manager",
    version="1.00",
    author="Alex Negrete",
    author_email="l@mail.com",
    packages=find_packages(),
    long_description=open("README.md").read(),
)
